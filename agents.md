# weight-maintain-clinic｜專案藍圖

減重門診**維持期回診**紀錄工具。輸入工具（單頁 HTML）→ Google 表單「\*體重維持紀錄」
→ 回覆試算表自動彙整同一位個案的體重歷程。

參考來源：既有的減重門診輸入工具 <https://philia81301-commits.github.io/weight-clinic/>
與 Google 表單「\*減重評估表」。**改動前先讀本檔「不可違反」與「踩過的坑」兩節。**

## 檔案

| 檔案 | 用途 |
|---|---|
| `index.html` | 主工具（單頁、原生 JS、無外部相依） |
| `tools/create-google-form.gs` | Apps Script：一鍵建立表單＋回覆試算表＋歷程分頁，並印出 `FORM_ID`／`ENTRY` |
| `docs/google-form-fields.md` | 表單欄位對照表與建表注意事項 |
| `_local/` | 臨床資料（**已 gitignore，絕不進版控**） |

## 定位：和「\*減重評估表」的分工

| | \*減重評估表（既有） | \*體重維持紀錄（本專案） |
|---|---|---|
| 時機 | 初診一次性評估 | 每次回診 |
| 內容 | 減重動機、期望體重、過去減重經驗、自費意願、費用 | 體重、用藥＋劑量、飲食、運動 |
| 個案鍵 | 無（僅姓名） | 姓名＋西元出生年 |

初診評估的題目**不要**搬進本表單，每次回診重問沒意義，也會讓回覆試算表塞滿空欄。

## 資料來源的時間軸（決定了本工具的定位）

| 來源 | 涵蓋期間 | 內容 | 角色 |
|---|---|---|---|
| 每月上傳的 `減重門診個案YYYY_M.xls` | **2024/1/1 起** | 每位個案的初診(v1)與最新回診(v2)，**每人只有兩個時間點** | 歷史資料，月報統計用 |
| `*減重評估表`（Google 表單） | **2025/9 起** | 初診評估（動機、期望體重、費用意願等） | 初診，較晚才開始，早期個案沒有 |
| `*體重維持紀錄`（本專案） | **2026/8 起** | 每次回診的體重、用藥劑量、飲食、運動 | **未來的主要資料來源** |

**2025/9 之前的個案沒有評估表資料，補不回來**（使用者 2026-08-18 說明）。
xls 每人也只存 v1／v2 兩個時間點，中間的回診沒有留下。

**因此：不要為了「補齊歷史」而把本工具的結構遷就 xls。**
往後以本表單累積的多次回診紀錄為主，xls 是既有的歷史底稿。
真要跨來源比對時，用 `chart`（病歷號）當 join key——但本表單目前不收病歷號
（個案鍵是姓名＋出生年，見下），跨來源比對需要另外處理。

## 不可違反

1. **性別編碼 `0 = 女`、`1 = 男`**。分析前用平均身高交叉驗證
   （2026_7.xls 實測：0 組 160.8 cm／n=459，1 組 173.0 cm／n=329）。
2. **n < 10 的組別不當亮點呈現**，可列附表，不寫進結論。
3. **用藥與劑量一律用下拉，不留自由文字**。資料庫 `drug` 欄是自由文字，同時混用數字代碼
   與各種拼寫（`moun`／`mou`／`M`／`Mounjaro`、`wegovy`／`wegovey`）。這是本專案改用下拉的
   **唯一理由**，不要為了「彈性」改回文字輸入。
   **舊資料的歸類一律 eval `analyze_case.js` 的 `normalizeDrug()`，不要自己另寫一份**
   （全域 skill 明文指示，避免與官方月報口徑不一致）。位置：
   `~/.local/share/chezmoi/dot_claude/tools/weight-clinic/executable_analyze_case.js`

   **用藥代碼表**（使用者 2026-07-05 提供，勿再詢問）：

   | 代碼 | 藥 | | 代碼 | 藥 |
   |---|---|---|---|---|
   | `0` | 無用藥 | | `4` | Saxenda |
   | `1` | Contrave | | `5` | Xenical |
   | `2` | Jardiance | | `6` | 併用兩種 |
   | `3` | Ozempic | | `7` | 手術 |

   文字 `Rybelsus` 原樣；`Moun`／`M` = Mounjaro。
   **代碼 `7` 與 `bariatric`／`GS`／`ESG`／`外科` 同義，一律歸「手術」**
   （2026-08-18 使用者更正；全域 skill 與兩支 `normalizeDrug()` 都已同步改掉
   原本的「代碼 7 不存在」）。
   **「無用藥」有三種等價寫法：`0`、空白、`lifestyle`** —— 都代表確實未使用減重藥物、
   **非漏登，不需再向使用者確認**（2026-07-19 使用者說明）。

4. **個案鍵是姓名＋西元出生年**。771 筆個案中有 3 組真的同名不同人（病歷號不同），
   純用姓名會把不同人的歷程合併。
5. **展示個案只用近一年內仍有回診紀錄者**。失聯個案的數據留在統計裡，但不做衛教素材。
6. **手術個案不是本工具的服務對象**。減重門診不追蹤手術個案（2026-08-18 使用者指示），
   故用藥下拉刻意不列「手術」，測試名單也已排除。曾經加過又撤掉，不要再加回來。
7. **本工具只寫不讀**。歷程一律在回覆試算表查看。要讓公開頁面讀回歷程，就必須把個案
   資料開放給那個公開頁面（發布 CSV 或帶 token 的 Web App），2026-08-18 已決定不做。

## 隱私：與 sarcopenia-clinic 的差異（刻意的，不要「修正」）

`sarcopenia-clinic` 的守則是不上傳姓名，只送代號。**本專案會上傳姓名**，因為：

- 減重門診本來就以姓名對個案，既有的「\*減重評估表」回覆試算表也是存真實姓名
- 歷程合併需要穩定的個案鍵

代價是回覆試算表含可識別資料，所以：試算表不得公開分享、不得發布成 CSV、
臨床資料檔一律放 `_local/`。

## 踩過的坑（都是實際發生過的）

-1. **QUERY 不能用沒出現在 `select` 裡的彙總函式排序**（2026-08-18）。
   把 `min/max(日期)` 從 select 移除改用陣列公式後，忘了同步改掉
   `order by max(...)`，整條 QUERY 報錯、被 `IFERROR` 吞掉，`個案總表` 一片空白。
   **錯誤處理一律用 `IFNA` 不用 `IFERROR`**：`IFNA` 只吞「查無資料」(#N/A)，
   語法錯誤等真正的問題會顯示出來，不會偽裝成「還沒有資料」。

0. **日期一律以「回診日期」欄為準，不要用 Timestamp**（2026-08-18）。
   Google 表單只記送出時間；補舊資料時全部同一天送出，歷程會擠成一堆同日的點，
   排序與差值全錯且不報錯。兩張分析分頁用 `IF(回診日期="", Timestamp, 回診日期)`。

1. **多區段表單的 `pageHistory`**（繼承自 weight-clinic）：Google 只採計 `pageHistory`
   宣告「走過」的區段答案，其餘靜默丟棄。減重門診那份有 6 個區段，沒帶此參數導致
   2026-07 起第 1 區段之後的欄位全部送丟。
   **本專案的對策：表單做成單一區段、不分支**，就完全不需要這個參數。
   運動細項改成各自加一個「無」選項，由工具自動填。
2. **必填的靜默拒收**：Google 表單的必填是伺服器端驗證，背景 `no-cors` 送出時缺一項
   會整筆拒收，而前端讀不到拒絕回應 → 看起來成功、實際什麼都沒留下。
   **對策：表單端全部不設必填，必填一律前端擋**。最壞情況是收到一筆欄位不全的紀錄
   （看得見、可補），而不是整筆無聲蒸發。
3. **選項字串必須一字不差**，含全形斜線（`併用／轉換兩種`）。對不上的選項 Google
   靜默丟棄那一題，不報錯。
4. **回應分頁絕不可用 `ss.getSheets()[0]` 定位**（2026-08-18 實際踩到）。
   `SpreadsheetApp.create()` 會先建一張預設空白表，`setDestination()` 之後才另外加上
   回應分頁，索引 0 很可能是那張空白表。公式指向空表 → `個案歷程`／`個案總表` 全白，
   而且被 `IFERROR` 吃掉、不報錯，又是一次靜默失敗。
   **正解：`sh.getFormUrl()` 只有連結表單的分頁才回傳網址**，見 `findResponseSheet()`。
   本專案的回應分頁實際名稱是英文 **`Form Responses 1`**（試算表以英文語系建立），
   別假設是「表單回應 1」。
5. **公式的欄位位置一律由 `colMap()` 讀表頭解析，不寫死欄位字母**（2026-08-18 導入）。
   表單題目順序一改，回應分頁欄位可能跟著換位，寫死字母會整排錯位且**靜默算錯**。
   改過題目順序或新增題目後，**務必重跑 `repair()`** 重建兩張分析分頁。
   2026-08-18 實測：**調整表單題目順序並不會移動試算表既有欄位**（欄位順序在分頁
   建立當下就固定），所以表單順序與試算表欄位順序不一致是正常的，不要手動「修正」公式。
6. **entry ID 拿不到就別手工挖**：`item.getId()` 不是 `entry.N`。唯一可靠做法是為每題
   單獨造「只填這題」的預填連結再解析網址，`tools/create-google-form.gs` 已實作。
   注意 `withItemResponse()` 回傳新物件，沒接回傳值會拿到空的預填網址。

## 環境

這台電腦的使用者是 `office`，全域指令寫的 `C:\Users\phili\OneDrive\文件` **在這台不存在**。
Google Drive 一律走 MCP 連接器，不要預設 `G:\` 掛得起來。

## 路線圖

- [x] 讀通 weight-clinic 架構、確認 `*減重評估表` 欄位與資料庫 `drug` 欄現況
- [x] 決定個案鍵（姓名＋出生年）與歷程呈現方式（只在試算表）
- [x] `tools/create-google-form.gs`：建表單＋試算表＋個案歷程／個案總表
- [x] `index.html`：輸入工具（劑量隨藥連動、前端必填、單一區段送出）
- [x] 挑出回診 3 個月以上個案名單供手動測試
- [ ] 執行 `buildAll()`，把 `FORM_ID`／`ENTRY` 貼進 `index.html`
- [ ] 端到端測試：送出 → 試算表出現 → 歷程分頁數字正確
- [ ] git init + GitHub repo + Pages 上線（**對外動作，需使用者確認**）
